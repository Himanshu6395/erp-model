# =============================================================================
# API URL — only ONE of the two lines below must be active (no # in front).
#
# Local (laptop): keep localhost active, keep production commented.
# Before push / production build: comment localhost (# ...), uncomment production.
# =============================================================================

VITE_API_URL=http://localhost:5000

# VITE_API_URL=http://43.205.113.119:5000
