import StudentDirectoryPanel from "../../components/admin/StudentDirectoryPanel";

/**
 * Lists all enrolled students — same styling as the former directory block on the create page.
 */
function RegisteredStudentsPage() {
  return <StudentDirectoryPanel />;
}

export default RegisteredStudentsPage;
