# Copy to frontend/.env if needed. Prefer editing frontend/.env in the repo.
#
# Same as frontend/.env: use exactly one VITE_API_URL (comment the other).

# VITE_API_URL=http://localhost:5000

VITE_API_URL=http://43.205.113.119:5000
